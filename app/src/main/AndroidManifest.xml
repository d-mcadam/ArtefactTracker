<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artefacttrackerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.DatabaseOptionsActivity"></activity>
        <activity android:name=".activities.CollectionLogActivity" />
        <activity android:name=".activities.CollectorActivity" />
        <activity android:name=".activities.CollectionActivity" />
        <activity android:name=".activities.MaterialOptionsActivity" />
        <activity android:name=".activities.AddArtefactActivity" />
        <activity android:name=".activities.InventoryManagementActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>